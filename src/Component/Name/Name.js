import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Name = ({hl}) => {
    // const {0, 1, 2, 3} = hl.options;
    console.log(hl);
    
    return (
        <div>
          {/* <p>{0}</p> */}
          {/* <p><small>{id}</small></p> */}
        </div>
    );
};

export default Name;